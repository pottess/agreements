import type { ReactNode } from 'react'
export interface Column<T>{key:string;label:string;render:(item:T)=>ReactNode}
export function DataTable<T extends {id:string}>({columns,rows,selected=[],onToggle,onToggleAll}:{columns:Column<T>[];rows:T[];selected?:string[];onToggle?:(id:string)=>void;onToggleAll?:()=>void}){return <div className="table-wrap"><table className="table"><thead><tr>{onToggle&&<th><input type="checkbox" checked={rows.length>0&&selected.length===rows.length} onChange={onToggleAll}/></th>}{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.id}>{onToggle&&<td><input type="checkbox" checked={selected.includes(row.id)} onChange={()=>onToggle(row.id)}/></td>}{columns.map(c=><td key={c.key}>{c.render(row)}</td>)}</tr>)}</tbody></table>{!rows.length&&<div className="empty">Nenhum item encontrado para os filtros selecionados.</div>}</div>}
