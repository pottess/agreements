import type { ReactNode } from 'react';import { IconButton } from './Button'
export function Modal({title,children,onClose,actions}:{title:string;children:ReactNode;onClose:()=>void;actions?:ReactNode}){return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="card modal"><div className="drawer-head"><h2>{title}</h2><IconButton onClick={onClose}>×</IconButton></div>{children}{actions&&<div className="modal-actions">{actions}</div>}</section></div>}
export function Drawer({title,children,onClose}:{title:string;children:ReactNode;onClose:()=>void}){return <aside className="drawer"><div className="drawer-head"><h2>{title}</h2><IconButton onClick={onClose}>×</IconButton></div>{children}</aside>}
