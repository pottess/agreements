import { useState, type ReactNode } from 'react';import { Card } from '../../design-system/components/Card';import { ProgressBar } from '../../design-system/components/Feedback'
export function OwnerGroupCard({initialOpen=false,initials,name,subtitle,progress,values,children}:{initialOpen?:boolean;initials:string;name:string;subtitle:string;progress:number;values:{label:string;value:string}[];children:ReactNode}){const[open,setOpen]=useState(initialOpen);return <Card className="group"><button className="group-header" onClick={()=>setOpen(v=>!v)}><span className="avatar">{initials}</span><span><strong>{name}</strong><small>{subtitle}</small></span><span><small>Execução {progress}%</small><ProgressBar value={progress}/></span>{values.slice(0,3).map(v=><span key={v.label}><small>{v.label}</small><strong>{v.value}</strong></span>)}<span>{open?'⌄':'›'}</span></button>{open&&<div className="group-body">{children}</div>}</Card>}
