import { Drawer } from '../../design-system/components/Overlay';import type { Agreement } from '../../domain/agreements.types';import { brl } from '../../domain/agreements.selectors';import { StatusBadge,AutomationTypeBadge } from './StatusBadge'
export function DetailDrawer({agreement,onClose}:{agreement:Agreement;onClose:()=>void}){return <Drawer title={`${agreement.id} · ${agreement.lever}`} onClose={onClose}><div className="audit"><div><small>Rede / comprador</small><strong>{agreement.network} · {agreement.buyer}</strong></div><div><small>Tipo de apuração</small><AutomationTypeBadge type={agreement.automation}/></div><div><small>Valor orçado</small><strong>{brl(agreement.budget)}</strong></div><div><small>Status atual</small><StatusBadge status={agreement.status}/></div><div><small>Última alteração</small><strong>{agreement.updatedAt}</strong></div>{agreement.exceptionReason&&<div><small>Justificativa da exceção</small><strong>{agreement.exceptionReason}</strong></div>}</div></Drawer>}
