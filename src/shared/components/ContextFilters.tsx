import type { ReactNode } from 'react';import { Card } from '../../design-system/components/Card'
export function ContextFilters({children}:{children:ReactNode}){return <Card className="filters">{children}</Card>}
