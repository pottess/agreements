import type { ReactNode } from 'react'
export function PageHeader({title,description,eyebrow,actions,rightSlot}:{title:string;description:string;eyebrow?:string;actions?:ReactNode;rightSlot?:ReactNode}){return <header className="page-header"><div>{eyebrow&&<small>{eyebrow}</small>}<h1>{title}</h1><p>{description}</p></div>{(rightSlot||actions)&&<div className="page-header-actions">{rightSlot}{actions}</div>}</header>}
