import { useMemo,useState } from 'react';import { Button } from '../../design-system/components/Button';import { MetricCard,Card } from '../../design-system/components/Card';import { DataTable,type Column } from '../../design-system/components/DataTable';import { PageHeader } from '../../shared/components/PageHeader';import { TableToolbar,BulkActionsBar } from '../../shared/components/TableParts';import { StatusBadge,AutomationTypeBadge } from '../../shared/components/StatusBadge';import { DetailDrawer } from '../../shared/components/DetailDrawer';import { useAgreements } from '../../domain/agreements.store';import { brl } from '../../domain/agreements.selectors';import type { Agreement } from '../../domain/agreements.types'
export function ApprovalPage(){const{state,dispatch}=useAgreements();const[search,setSearch]=useState('');const[selected,setSelected]=useState<string[]>([]);const[detail,setDetail]=useState<Agreement>();const rows=useMemo(()=>state.agreements.filter(a=>a.stage==='Aprovação'&&(a.network+a.buyer+a.lever).toLowerCase().includes(search.toLowerCase())),[state.agreements,search]);const toggle=(id:string)=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);const decision=(d:'approve'|'reject'|'adjust')=>dispatch({type:'PPM_DECISION',ids:selected,decision:d});const columns:Column<Agreement>[]=[{key:'name',label:'Rede · Comprador',render:a=><><strong>{a.network}</strong><small>{a.buyer}</small></>},{key:'lever',label:'Alavanca',render:a=>a.lever},{key:'auto',label:'Apuração',render:a=><AutomationTypeBadge type={a.automation}/>},{key:'budget',label:'Orçado',render:a=>brl(a.budget)},{key:'measured',label:'Apurado',render:a=>brl((a.diValue??0)+(a.realValue??0))},{key:'real',label:'Valor real',render:a=>a.realValue?brl(a.realValue):'—'},{key:'proposed',label:'Proposto',render:a=><strong>{brl(a.proposedValue??a.budget)}</strong>},{key:'kam',label:'Confirmação KAM',render:a=><span>{a.kamConfirmed??'—'}</span>},{key:'status',label:'Status',render:a=><StatusBadge status={a.status}/>},{key:'decision',label:'Decisão PPM',render:a=><Button size="sm" onClick={()=>dispatch({type:'PPM_DECISION',ids:[a.id],decision:'approve'})}>Aprovar</Button>},{key:'actions',label:'Ações',render:a=><Button size="sm" variant="tertiary" onClick={()=>setDetail(a)}>Histórico</Button>}];return <div className="page"><PageHeader title="Aprovação" description="Aprove, rejeite ou solicite ajustes nos acordos confirmados pelo KAM." actions={<Button onClick={()=>decision('approve')}>Aprovar selecionados</Button>}/><div className="metrics"><MetricCard label="Aguardando aprovação" value={String(rows.filter(a=>a.status==='Aguardando aprovação').length)}/><MetricCard label="Valor orçado" value={brl(rows.reduce((n,a)=>n+a.budget,0))}/><MetricCard label="Valor apurado" value={brl(rows.reduce((n,a)=>n+(a.diValue??0)+(a.realValue??0),0))}/><MetricCard label="Valor proposto" value={brl(rows.reduce((n,a)=>n+(a.proposedValue??a.budget),0))}/><MetricCard label="Rejeições / ajustes" value={String(rows.filter(a=>a.status==='Ajuste solicitado').length)} icon="!"/></div><Card><TableToolbar search={search} onSearch={setSearch}/><BulkActionsBar count={selected.length}><Button size="sm" variant="tertiary" onClick={()=>decision('approve')}>Aprovar</Button><Button size="sm" variant="tertiary" onClick={()=>decision('adjust')}>Solicitar ajuste</Button><Button size="sm" variant="danger" onClick={()=>decision('reject')}>Rejeitar</Button></BulkActionsBar><DataTable rows={rows} columns={columns} selected={selected} onToggle={toggle} onToggleAll={()=>setSelected(selected.length===rows.length?[]:rows.map(r=>r.id))}/></Card>{detail&&<DetailDrawer agreement={detail} onClose={()=>setDetail(undefined)}/>}</div>}
