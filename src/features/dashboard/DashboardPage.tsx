import { Card,MetricCard } from '../../design-system/components/Card';import { PageHeader } from '../../shared/components/PageHeader';import { useAgreements } from '../../domain/agreements.store';import { brl,totals } from '../../domain/agreements.selectors'
export function DashboardPage(){const{state}=useAgreements(),t=totals(state),di=state.agreements.filter(a=>a.automation==='DI').length;return <div className="page"><PageHeader title="Dashboard executivo" description="Visão dos acordos por status e vigência."/><div className="metrics"><MetricCard label="Planejado anual" value={brl(t.planned)}/><MetricCard label="Orçado" value={brl(t.budget)}/><MetricCard label="Apurado" value={brl(t.measured)}/><MetricCard label="Pago" value={brl(t.paid)}/><MetricCard label="DI" value={String(di)}/></div><Card><div className="empty">Acompanhe os status operacionais diretamente na tela Acordos.</div></Card></div>}
