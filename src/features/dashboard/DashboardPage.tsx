import { MetricCard,Card } from '../../design-system/components/Card';import { PageHeader } from '../../shared/components/PageHeader';import { ContextFilters } from '../../shared/components/ContextFilters';import { Select } from '../../design-system/components/Fields';import { ProgressBar } from '../../design-system/components/Feedback';import { StatusBadge } from '../../shared/components/StatusBadge';import { useAgreements } from '../../domain/agreements.store';import { brl,totals } from '../../domain/agreements.selectors'
export function DashboardPage(){const{state}=useAgreements(),t=totals(state);const auto=state.agreements.filter(a=>a.automation==='Automática DI').length/state.agreements.length*100;return <div className="page"><PageHeader title="Dashboard executivo" description="Visão ponta a ponta dos valores, gargalos e acordos que precisam de ação."/><ContextFilters><Select label="Mês"><option>Março 2026</option></Select><Select label="Rede"><option>Todas as redes</option></Select><Select label="KAM"><option>Todos os KAMs</option></Select><Select label="PPM"><option>Todos os PPMs</option></Select><Select label="Regional"><option>Brasil</option></Select></ContextFilters><div className="metrics"><MetricCard label="Planejado anual" value={brl(t.planned)}/><MetricCard label="Orçado" value={brl(t.budget)}/><MetricCard label="Apurado" value={brl(t.measured)}/><MetricCard label="Aprovado" value={brl(t.approved)}/><MetricCard label="Pago" value={brl(t.paid)} icon="✓"/></div><div style={{display:'grid',gridTemplateColumns:'1.2fr .8fr',gap:8}}><Card className="metric" ><div style={{width:'100%'}}><div className="section-title"><div><h2>Funil de valores</h2><p>Onde o valor mudou ao longo do processo.</p></div></div>{[['Planejado',t.planned],['Orçado',t.budget],['Apurado',t.measured],['Aprovado',t.approved],['Pago',t.paid]].map(([label,value])=><div key={label as string} style={{display:'grid',gridTemplateColumns:'90px 1fr 110px',gap:8,alignItems:'center',margin:'12px 0'}}><span>{label}</span><ProgressBar value={(value as number)/t.planned*100}/><strong>{brl(value as number)}</strong></div>)}</div></Card><Card style={{padding:16}}><div className="section-title"><div><h2>Automático vs manual</h2></div></div><strong style={{fontSize:28}}>{Math.round(auto)}%</strong><p>Automática DI</p><ProgressBar value={auto}/><p style={{marginTop:18}}>{Math.round(100-auto)}% manual ou híbrida</p></Card></div><div className="section-title"><div><h2>Acordos parados</h2><p>Itens de maior impacto para atuação.</p></div></div><Card><table className="table"><thead><tr><th>Acordo</th><th>Rede</th><th>Etapa</th><th>Status</th><th>Responsável</th><th>Valor</th></tr></thead><tbody>{state.agreements.filter(a=>['Manual pendente','Com divergência','Aguardando assinatura','Saldo em aberto'].includes(a.status)).map(a=><tr key={a.id}><td><strong>{a.id}</strong><small>{a.lever}</small></td><td>{a.network}</td><td>{a.stage}</td><td><StatusBadge status={a.status}/></td><td>{a.kam}</td><td>{brl(a.approvedValue??a.budget)}</td></tr>)}</tbody></table></Card></div>}
